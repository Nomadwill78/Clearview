project_id = "celestial"

# Stripe calls this endpoint directly and cannot send a Supabase JWT;
# authenticity is enforced by Stripe signature verification inside the function.
[functions.stripe-webhook]
verify_jwt = false
